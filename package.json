{
  "name": "contactlogo",
  "private": true,
  "license": "Apache-2.0",
  "description": "ContactLogo — brand icons for your address book",
  "homepage": "https://contactlogo.com",
  "scripts": {
    "dev": "npm run dev --prefix web",
    "build": "npm run build --prefix web",
    "preview": "npm run preview --prefix web",
    "typecheck": "npm run typecheck --prefix web",
    "test": "npm test --prefix web",
    "test:web": "npm test --prefix web",
    "test:swift": "swift test"
  }
}
