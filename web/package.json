{
  "name": "badgebook-web",
  "private": true,
  "license": "Apache-2.0",
  "description": "BadgeBook Web — review-first logo matching for a vCard or Google CSV",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types --test src/engine/*.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^6.2.0"
  }
}
